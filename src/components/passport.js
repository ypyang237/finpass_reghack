import React, { Component } from 'react';
import GlobalHeader from './globalHeader.js'

const Passport = ({  }) => (
  <div>
    <GlobalHeader />
    <h3>PASSPORT</h3>
  </div>
)

export default Passport;